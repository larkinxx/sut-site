# ИННфакт (fin-check.shop) — передача проекта

Документ для нового помощника (ChatGPT или другого ИИ) и для разработчика. Состояние на 25 сентября 2026 г.
Код: https://github.com/larkinxx/sut-site (ветка `main`). Владелец — Владимир (CEO), общается по-русски.

## 1. Что это за проект

**ИННфакт** — сервис проверки компаний и ИП по ИНН с понятным разбором и советами бизнесу.
Основа — проверка организаций; вокруг — экономические новости с разбором «что это значит для вас»,
финансовые калькуляторы и раздел о законном снижении налогов. Слоган: **«Введите ИНН — узнайте суть»**.

Позиционирование: не «ещё один Rusprofile» (у него 38 источников — по полноте данных не конкурировать),
а сервис для **владельца малого бизнеса**: не пересказ реестра, а следующий шаг — риски, что проверить,
как законно платить меньше налогов, за чем следить. Подробное сравнение: `Claude outputs/sut-vs-konkurenty.pdf`.

## 2. Правила, которые нельзя нарушать

- **Язык и тон**: весь текст сайта по-русски, простым языком, без канцелярита.
- **Слово «ИИ» не выносить в заголовки и маркетинг** — владелец считает, что это удешевляет проект.
  Честные пометки оставлены: «подготовлено автоматически», раскрытие передачи данных в ИИ-сервис мелким шрифтом,
  объяснение на странице «Как мы работаем».
- **Оценка надёжности — только как мнение по данным**: индекс ИННфакт 0–100 (`innfactIndex` в `public/org.js`, методика `/indeks/`), всегда с факторами; без слов «мошенники», «однодневка», без обвинений; без обещаний дохода и «покупайте/продавайте»
  (проверки в `src/lib/schema.mjs` → `FORBIDDEN`, и в `validateAi` на сервере).
- **Налоги — только законные способы.** Схемы (дробление, «технические» фирмы, сотрудники как самозанятые и т. п.)
  описываются только как предупреждение. ИИ-ответы с советами схем отсекаются в `validateAi`.
- **152-ФЗ**: персональные данные пользователей (аккаунты) хранятся только на сервере в России (Timeweb).
  ФИО/ИНН руководителей и учредителей из «Прозрачного бизнеса» наружу не отдаём — только количества.
- **Капчи не обходить** (pb.nalog.ru) — при капче сервер делает паузу 2 часа.
- **Секреты** (ключи API, пароли) владелец вписывает сам на сервер; в чат и в репозиторий их не класть.

## 3. Архитектура и где что работает

| Что | Где | Примечание |
|---|---|---|
| Сайт (статический, собирается `src/build.mjs` → `dist/`) | Timeweb **App Platform**, приложение **«Cute Hoopoe»** | Пересобирается сам при пуше в `main` (workflow `.github/workflows/timeweb.yml`) |
| API-сервер (`server/index.mjs`) | Timeweb **облачный сервер `sut-api`**, Новосибирск, IP **5.129.207.51**, адрес **https://api.fin-check.shop** | Ubuntu 24.04, Node 24, Caddy (HTTPS), systemd `sut-api`. Тариф 1 100 ₽/мес |
| Ретранслятор к Gemini | Render, **https://sut-api.onrender.com** (бесплатный, засыпает) | Нужен, пока разбор идёт через Google Gemini: Google не пускает к Gemini с российских IP. Обновляется сам из GitHub |
| DNS домена fin-check.shop | REG.RU, хостинг Host-0 u3655609 → панель **ispmanager** (server192.hosting.reg.ru:1500) | Записи: `api` → 5.129.207.51. Почта домена — там же |
| Сбор новостей | GitHub Actions `.github/workflows/pipeline.yml` (каждый час) | + обновление ставки ЦБ (`scripts/rates.mjs`) и публикация в Telegram (`scripts/telegram.mjs`, пока без ключей) |
| Аналитика | Яндекс Метрика, счётчик **113035472** | |

**Доступ к серверу**: SSH только по ключу `~/.ssh/sut_timeweb` на Mac владельца (вход по паролю выключен).
Команды владелец запускает сам, например:
```
ssh -i ~/.ssh/sut_timeweb root@5.129.207.51
nano /etc/sut/api.env                      # настройки и ключи сервера
sh /opt/sut-site/deploy/update.sh          # обновить сервер из GitHub и перезапустить
journalctl -u sut-api -f                   # логи
```
Инструкция по серверу: `deploy/README.md`. Пример настроек: `deploy/api.env.example`.

## 4. Структура репозитория

- `src/build.mjs` — сборка всех страниц (шаблон, новости, калькуляторы, раздел «Налоги», вход/кабинет, sitemap, RSS).
- `src/lib/` — `content.mjs` (загрузка контента), `schema.mjs` (проверка карточек новостей), `taxes.mjs` (тексты раздела «Налоги» и схем), `util.mjs`.
- `public/` — `app.js` (лента, калькуляторы, «Поделиться/Сохранить расчёт», виджет ставки ЦБ), `org.js` (проверка по ИНН, памятка, налоговые советы `taxIdeas`, блок ФНС), `account.js` (вход и кабинет), `style.css`, шрифты PT Serif, иконки, `og.png`.
- `server/index.mjs` — API: `/api/org`, `/api/org/ai`, `/api/org/fns`, `/api/org/suggest`, аккаунты.
- `server/accounts.mjs` — вход (Яндекс ID, Telegram, код на почту), кабинет, слежение за компаниями, SMTP-клиент.
- `server/fns.mjs` — данные ФНС: открытые данные (из `fns.db`), ГИР БО, «Прозрачный бизнес».
- `scripts/` — `fetch.mjs`, `draft.mjs` (новости), `rates.mjs` (ставка ЦБ), `telegram.mjs`, `fns-import.mjs` (импорт открытых данных ФНС, cron 26-го числа).
- `config/` — `site.json` (название, контакты, **operator** — пустой!), `finance.json` (все ставки и лимиты для калькуляторов и советов, сверены 25.09.2026), `sources.json`.
- `deploy/` — установка и обслуживание сервера.
- `Claude outputs/` (не в git) — логотипы, визитки (бумажная и металлические), PDF сравнения с конкурентами.

**Проверки перед пушем**: `npm run test:server` (39 тестов: сервер, аккаунты, ФНС), `npm run build`, `npm run check`.

## 5. Что уже работает на fin-check.shop

- Лента новостей за 24 ч с разбором по аудиториям, архив, карточки, RSS, виджет «ключевая ставка / инфляция / следующее решение ЦБ».
- Калькуляторы: кредит, досрочное погашение, вклад, налог на вклады, самозанятый или ИП; «Поделиться расчётом».
- **Проверка организации по ИНН** (через api.fin-check.shop): данные DaData, ссылки на реестры и ЦБ,
  экспресс-разбор (Алиса — Yandex AI Studio, модель aliceai-llm), памятка, налоговые советы владельцу,
  **блок «Финансы и налоги» по данным ФНС**: режим, численность, уплаченные налоги, налоговый долг с расшифровкой,
  отчётность за 5 лет с графиком, сигналы (убыток, падение выручки, отрицательный капитал, долг).
  Открытые данные ФНС в базе на сервере: 1,3 млн режимов, 2 млн — численность, 2,6 млн — налоги, 874 тыс. — долги.
- Раздел «Налоги»: законные способы по видам налогов, «Опасные налоговые схемы», «Блокировка счёта по 115-ФЗ»; «Не становитесь дропом».
- Логотип «С.» в кольце, favicon, иконки, превью для соцсетей.

## 6. Готово в коде, но ещё не включено

| Функция | Что нужно для включения |
|---|---|
| **Вход и кабинет** (Яндекс ID, Telegram, код на почту; история, «моя компания», расчёты, слежение с уведомлениями) | 1) заполнить `operator` в `config/site.json` (ФИО/название и ИНН оператора ПДн) — **заполнено**; 2) хотя бы один способ входа (см. ниже); 3) переменная `ACCOUNT_API_URL=https://api.fin-check.shop` в приложении сайта на Timeweb; 4) уведомление в Роскомнадзор (pd.rkn.gov.ru) |
| **Вход по почте** | Ящик `noreply@fin-check.shop` создан и вписан в `/etc/sut/api.env` (SMTP_HOST=server192.hosting.reg.ru). **Timeweb блокирует исходящие порты 25/465/587** — написать в поддержку Timeweb с просьбой открыть 465 для сервера sut-api (транзакционные письма с кодами). Либо почтовый сервис с HTTP-API |
| **Вход через Telegram и уведомления** | **Работает** (проверено 25.09.2026). Вход через бота @fin_check_sute_bot: кнопка на /vhod/ → `t.me/<бот>?start=<код>` → «Запустить» → кнопка «Войти на fin-check.shop» в боте → сайт сам переходит в кабинет (`POST /auth/telegram/start`, `GET /auth/telegram/status`). Виджет Telegram Login убран: в России oauth.telegram.org давал «Server error». Timeweb не пускает сервер к api.telegram.org, поэтому все запросы к боту идут через Render: `TELEGRAM_API_URL=https://sut-api.onrender.com/tg` в `/etc/sut/api.env` (маршрут `/tg/` в `server/index.mjs`, только методы бота). Первый вход после паузы — до 30 с (Render просыпается). Для публикации новостей в канал — секрет `TELEGRAM_BOT_TOKEN` и переменная `TELEGRAM_CHAT_ID` в GitHub |
| **Вход через Яндекс ID** | Приложение на oauth.yandex.ru, Redirect URI `https://api.fin-check.shop/auth/yandex/callback` → `YANDEX_CLIENT_ID`, `YANDEX_CLIENT_SECRET` |
| ~~Разбор через Алису~~ — **включён 25.09.2026** (каталог b1gusu9t8am4vkm07rk8). Осталось убрать из `/etc/sut/api.env` `GEMINI_API_KEY` и `AI_UPSTREAM_URL`, удалить ключ Gemini в aistudio.google.com; ретранслятор Render больше не нужен | Код готов и протестирован. Нужен каталог Yandex Cloud, сервисный аккаунт с ролью `ai.languageModels.user`, API-ключ → `YANDEX_AI_KEY`, `YANDEX_FOLDER_ID` (модель по умолчанию `aliceai-llm`, дешевле — `aliceai-llm-flash`). После этого убрать `AI_UPSTREAM_URL` из `/etc/sut/api.env`, `sh deploy/update.sh`. Поменять упоминание Google Gemini в тексте под формой проверки (`src/build.mjs`, страница `organizacii`) и в политике конфиденциальности на «Яндекс (Alice AI)». Цена ≈ 0,5 ₽ / 1,2 ₽ за 1000 токенов ввода/вывода |

## 7. Следующие задачи (по приоритету)

1. ~~Интерфейс поиска по названию~~ — **сделано** (коммит 944a725): поле «ИНН или название», подсказки от 3 символов,
   стрелки/Enter/Esc, поиск кнопкой. Заработает на сайте после обновления сервера: `sh /opt/sut-site/deploy/update.sh`
   (до этого поле работает только по ИНН, а на поиск по названию пишет «сейчас недоступен»).
2. ~~Включить вход~~ — **сделано**: вход через Telegram работает, почта скрыта (`#SMTP_HOST` в api.env), пока Timeweb блокирует порты. Осталось: Яндекс ID как второй способ, уведомление в Роскомнадзор.
3. ~~Переключить разбор на Алису~~ — сделано.
4. ~~Суды, приставы, банкротства~~ — **сделано** через DataNewton (`server/datanewton.mjs`, `POST /api/org/more`, ключ `DATANEWTON_KEY` на сервере): подробная карточка (руководство, учредители, капитал, все ОКВЭД, численность, контакты компании, отметки в реестрах), суды общей юрисдикции, арбитраж с исходами, приставы; проверка сайтов компании (`server/site-check.mjs`: ИНН на сайте, HTTPS, возраст домена, соцсети). 4 единицы лимита на компанию, кеш сутки, `DATANEWTON_DAILY` (по умолчанию 150 компаний в сутки). Сейчас пробный доступ — нужен платный тариф. Результаты проверки — сеткой карточек на всю ширину (`public/org.js`, стили `.dash`/`.dcard`). Личные контакты людей и ИНН физлиц не показываем.
5. История изменений компании в карточке (снимки уже собираются для слежения: таблица `snapshots` в `server/accounts.mjs`).
6. Калькуляторы: налоговый вычет, пени, НДС на УСН, отпускные. Налоговый календарь для бизнеса.
7. Настройки хостинга сайта в Timeweb («Настройки веб-сервера»): сжатие, кеш, настоящая 404, редирект с www.
8. Металлическая визитка: макет готов (`Claude outputs/vizitka-metal-v2/`), ждёт подтверждения фамилии и отправки в мастерскую.

## 8. Важно помнить

- **Баланс Timeweb** на 25.09.2026 — ~438 ₽, хватает примерно **до 3 октября**. Если не пополнить — остановятся сайт и сервер.
- ГИР БО и открытые данные ФНС работают с российских IP; Gemini — нет (поэтому ретранслятор на Render).
- `config/finance.json` — единый источник ставок/лимитов на 2026 год; ставка ЦБ и инфляция обновляются сами (`scripts/rates.mjs`),
  даты заседаний ЦБ на следующий год добавить вручную.
- Если сайт не видит изменения сервера — сервер на Timeweb обновляется **только вручную** (`deploy/update.sh`),
  Render и сайт — автоматически из GitHub.

## 9. Состояние на 26.09.2026, день

**Сайт переехал: название «ИННфакт», адрес https://innfact.ru** (домен в REG.RU, DNS-серверы ns1/ns2.reg.ru, записи @, www, api → 5.129.207.51).
Сайт и API живут на своём сервере (Timeweb, 5.129.207.51): Caddy (`deploy/Caddyfile.site`), сборка `deploy/site-build.sh` каждые 10 минут по cron,
настройки `/etc/sut/site.env` и `/etc/sut/api.env` (переключаются `deploy/move-domain.sh`). Старый fin-check.shop и www — постоянный редирект 301 на innfact.ru,
api.fin-check.shop продолжает работать. Приложение «Cute Hoopoe» на Timeweb App Platform больше не нужно.

Страницы компаний `/organizacii/<ИНН>/` (данные ФНС + DaData), картинки-превью `/organizacii/<ИНН>/og.png` (rsvg-convert), карта `/sitemap-companies.xml`.
Логотип: кольцо «Ф.» и словесный знак «ИННфакт.» — `Claude outputs/logo-innfact/`, визитка — `Claude outputs/vizitka-metal-innfact/`.

Осталось:
1. Яндекс Вебмастер: добавить https://innfact.ru (код подтверждения — в `config/site.json` → verification), Sitemap `/sitemap.xml` и `/sitemap-companies.xml`; в старых сайтах fin-check.shop — «Переезд сайта» на innfact.ru. То же в Google Search Console.
2. Выключить приложение «Cute Hoopoe» на Timeweb и workflow `.github/workflows/timeweb.yml`.
3. Telegram: в @BotFather `/setname` → «ИННфакт — проверка компаний», `/setuserpic` → `icon-512.png`.
4. Метрика: адрес сайта в настройках счётчика 113035472 поменять на innfact.ru.
5. DataNewton: пробный лимит почти израсходован — нужен тариф.
